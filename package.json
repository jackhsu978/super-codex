{
  "name": "star-sprint-platformer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "npm run check:levels && tsc && vite build",
    "check:levels": "node scripts/validate-level-safety.mjs",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^8.0.16"
  }
}
